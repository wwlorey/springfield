Study the project structure, README, and any setup instructions (package.json, Cargo.toml, Makefile, etc.). Install all dependencies and ensure the project builds successfully.

Run the build and test commands from BACKPRESSURE.md to verify everything works. Fix any issues you encounter.

When finished, touch `.ralph-complete`.
